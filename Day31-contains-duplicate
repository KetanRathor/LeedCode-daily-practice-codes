var containsDuplicate = function(nums) {
    nums.sort((a,b)=>a-b);
    for(let i=0;i<nums.length;i++){
        // for(let j =i+1;j<nums.length; j++){
            if(nums[i]===nums[i+1]){
                return true;
            }
            
           
        }
         
    
    return false;
    
};


// We Can Set() also we can store the values in Set() and campare the size of set and num length
